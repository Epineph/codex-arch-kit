# Job 2: useful help and comments

Improve selected scripts' --help text: describe accepted inputs, defaults,
examples and relevant errors. Keep the command names and current argument
contracts. Comment non-obvious behaviour, such as counting newline characters
rather than logical text records or preserving first-seen line order.
Do not comment every line or add dependencies. Do not add new options.
Help wording may change, but --help must still exit successfully.
Run python3 lab.py check. Stop and show the diff for review.
